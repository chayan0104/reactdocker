function Header() {
  return (
    <header
      style={{
        background: "#282c34",
        padding: "20px",
        color: "white",
        textAlign: "center",
        fontSize: "26px",
        fontWeight: "bold",
      }}
    >
      React + Docker Learning Project
    </header>
  );
}

export default Header;
